apiVersion: apps/v1
kind: Deployment
metadata:
  name: clinical-service-deployment
  namespace: genosentinel
  labels:
    app: clinical-service
    component: microservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: clinical-service
  template:
    metadata:
      labels:
        app: clinical-service
        component: microservice
    spec:
      containers:
      - name: clinical-service
        image: genosentinel-clinical-service:latest
        ports:
        - containerPort: 3020
        envFrom:
        - configMapRef:
            name: clinical-config
        - secretRef:
            name: database-secrets
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: mysql-password
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3020
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3020
          initialDelaySeconds: 5
          periodSeconds: 5